# ── 子项目（各自是独立 git repo，不在本仓库追踪）──────────────
Guardrails/
PyRIT/
llm-guard/
promptfoo/
eval_frameworks/deepeval/
eval_frameworks/inspect_ai/
eval_frameworks/helm/
eval_frameworks/mlcommons-ailuminate/
eval_frameworks/mlcommons-modelbench/

# ── 评测输出（当前版本需要完整留痕入库，供复盘与审计）──────────
# 注意：raw/normalized 已改为纳入版本管理

# ── Python ────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
*.pyo
.eggs/

# ── Node.js ───────────────────────────────────────────────────
node_modules/

# ── 环境与密钥 ────────────────────────────────────────────────
.env
.env.*
*.local
*.secret

# ── macOS ─────────────────────────────────────────────────────
.DS_Store
.AppleDouble

# ── Claude Code 本地设置（机器私有）────────────────────────────
.claude/settings.local.json

# ── 编辑器 ────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo

.claude/
workflow_history.md
